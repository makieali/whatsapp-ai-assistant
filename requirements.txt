Flask==3.0.3
openai>=1.55,<3
python-dotenv==1.0.1
requests==2.32.3
Werkzeug==3.0.3
SQLAlchemy>=2.0,<3
alembic>=1.13,<2
# Postgres driver — only needed when DATABASE_URL points at PostgreSQL.
psycopg[binary]>=3.1

# Dev / test
pytest==8.2.2
pytest-cov==5.0.0
responses==0.25.3
